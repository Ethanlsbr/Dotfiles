general {
    lock_cmd = hyprlock --grace 0           # dbus/sysd lock command (loginctl lock-session)
    before_sleep_cmd = hyprlock --grace 0   # command ran before sleep
    after_sleep_cmd = hyprlock --grace 0    # command ran after sleep
    ignore_dbus_inhibit = false             # whether to ignore dbus-sent idle-inhibit requests (used by e.g. firefox or steam)
    ignore_systemd_inhibit = false          # whether to ignore systemd-inhibit --what=idle inhibitors
}
