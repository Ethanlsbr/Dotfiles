[general]
bars = 32
framerate = 60
autosens = 1
sensitivity = 100

[output]
method = raw
raw_target = /dev/stdout
data_format = ascii
ascii_max_range = 1000

[smoothing]
# Lower = snappier / more movement (0.77 was heavily damped).
noise_reduction = 0.30
